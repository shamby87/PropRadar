"""PropRadar performance dashboard.

Reads PropRadar-only betting results from Google Sheets, computes performance
analytics, and exports a static JSON payload consumed by the dashboard site in
``docs/``.
"""
